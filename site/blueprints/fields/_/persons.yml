# list of authors with website
# https://getkirby.com/docs/reference/panel/fields/structure

label: Associated Persons
type: structure

translate: false

fields:

  name:
    type: tags
    options: query
    icon: user
    max: 1
    query: page.siblings.pluck('persons',',',true)
