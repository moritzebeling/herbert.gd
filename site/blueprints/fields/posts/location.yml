# optional location
# https://getkirby.com/docs/reference/panel/fields/structure

type: structure
label: Associated Location

max: 1
translate: false
fields:

  name:
    type: tags
    label: Name
    width: 1/2
    icon: home
    options: query
    max: 1
    query: page.siblings.pluckStructure('location','name')

  city:
    type: tags
    label: Stadt
    max: 1
    width: 1/2
    icon: globe
    options: query
    query: page.siblings.pluckStructure('location','city')

columns:
  name:
    width: 1/2
  city:
    width: 1/2
