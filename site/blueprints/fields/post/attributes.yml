type: structure
columns:
  attribute:
    width: 1/2
  value:
    width: 1/2
fields:
  attribute:
    type: select
    options: query
    width: 1/2
    query:
      fetch: page.parent.attributes.toStructure
      text: "{{ structureItem.name }}"
      value: "{{ structureItem.name }}"
  value:
    type: tags
    width: 1/2
    options: query
    query: page.siblings.pluckStructure('attributes','value')
