# select other domains
# https://getkirby.com/docs/reference/panel/fields/checkboxes

type: checkboxes
label: Channels

translate: false
max: 3

options: query
query:
  fetch: site.children.listed
  text: "{{ page.title }}"
  value: "{{ page.slug }}"
